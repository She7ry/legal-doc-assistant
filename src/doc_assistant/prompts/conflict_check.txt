Task: Compare contract excerpts against policy excerpts and return a structured conflict matrix.

Contract excerpts:
<contract_documents>
{contract_context}
</contract_documents>

Policy excerpts:
<policy_documents>
{policy_context}
</policy_documents>

Allowed conflict types:
{conflict_types}

Use only the provided excerpts. Do not provide final legal advice. Do not cite statutes, cases, market standards, or external facts unless they appear in the excerpts.

Return exactly one JSON object. Do not wrap it in markdown. Use these keys:
- overall_status: one of No conflict found, Potential conflict, Insufficient information.
- conflicts: array of conflict objects. If no conflict is supported, return an empty array.
- needs_human_review: boolean.
- supporting_citations: array of source ids used to support a no-conflict or insufficient-information conclusion.

Each conflict object must include:
- topic: short label, such as Payment timeline, Data retention, Notice method, Confidentiality scope.
- conflict_type: one allowed snake_case key, such as deadline_mismatch or direct_contradiction.
- severity: one of Low, Medium, High, Needs human review.
- contract_position: concise statement of what the contract excerpt says.
- policy_position: concise statement of what the policy excerpt says.
- why_conflict: explain why the two positions may conflict, using only cited text.
- recommended_action: practical next step such as negotiate contract wording, request a policy exception, confirm with business owner, or ask attorney to review.
- contract_citations: array of contract source ids such as C1.
- policy_citations: array of policy source ids such as P1.
- needs_human_review: boolean.
- confidence: Low, Medium, or High.

Rules:
- Every conflict must include at least one contract citation and one policy citation.
- If one side has no excerpts, set overall_status to Insufficient information and explain that in the structured fields where possible.
- Soften legal conclusions. Use "may", "could", "appears", or "needs review" instead of definitive legal advice.

Answer:
