Task: Review the provided source excerpts for the requested clause type and return a structured risk assessment.

Requested clause type:
{clause_type}

Normalized clause type:
{normalized_clause_type}

Available clause taxonomy:
{clause_taxonomy}

Risk rules to apply:
{risk_rules}

Source excerpts:
<retrieved_documents>
{context}
</retrieved_documents>

Use only the provided excerpts. Do not provide final legal advice. Do not cite statutes, cases, market standards, or external facts unless they appear in the excerpts.

Return exactly one JSON object. Do not wrap it in markdown. Use these keys:
- clause_type: string.
- normalized_clause_type: taxonomy key or short normalized name.
- found: boolean if clear, otherwise null.
- summary: short plain-language summary grounded in cited excerpts.
- risk_level: one of Low, Medium, High, Needs human review.
- risk_reasons: array of objects. Each object must include reason and citation. Citation must be a source id such as S1, not a markdown link.
- affected_party: string or null.
- plain_language_explanation: explain what the clause means and why the user should pay attention.
- questions_for_lawyer: array of practical, non-conclusive questions the user could ask a lawyer or counterparty.
- missing_information: array of missing facts, sections, definitions, schedules, or jurisdiction details needed for stronger review.
- needs_human_review: boolean.

Rules:
- Every material claim must be tied to one or more source ids from the excerpts.
- If the clause was not found, set found to false, risk_level to Needs human review, and explain what source text is missing.
- Soften legal conclusions. Use "may", "could", "appears", or "needs review" instead of definitive legal advice.
- Prefer user-facing plain language while keeping the output valid JSON.

Answer:
