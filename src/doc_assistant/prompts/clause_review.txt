任务：审阅所请求条款类型的来源摘录，返回结构化风险评估。

请求条款类型：
{clause_type}

归一化条款类型：
{normalized_clause_type}

可用条款分类：
{clause_taxonomy}

适用风险规则：
{risk_rules}

来源摘录：
<retrieved_documents>
{context}
</retrieved_documents>

仅使用提供的摘录。不提供最终法律意见。不得引用摘录中未出现的法条、判例、市场标准或外部事实。

仅返回一个 JSON 对象，不要用 markdown 包裹。字段如下（键名保持英文）：
- clause_type: string。
- normalized_clause_type: 分类键或简短归一化名称。
- found: 明确则 boolean，否则 null。
- summary: 基于引用摘录的简短通俗摘要（中文）。
- risk_level: 取 Low、Medium、High、Needs human review 之一。
- risk_reasons: 对象数组，每项含 reason（中文）和 citation（来源 ID，如 S1，非 markdown 链接）。
- affected_party: string 或 null。
- plain_language_explanation: 条款含义及用户需关注的原因（中文）。
- questions_for_lawyer: 可向律师或对方提出的非结论性实际问题（中文）。
- missing_information: 加强审阅所需的缺失事实、章节、定义、附件或管辖信息（中文）。
- needs_human_review: boolean。

规则：
- 每项实质性陈述须关联一个或多个摘录中的来源 ID。
- 未找到条款时，设 found 为 false，risk_level 为 Needs human review，并说明缺失哪些来源文本。
- 软化法律结论，用「可能」「或会」「似乎」「需审阅」等，避免确定性法律意见。
- 面向用户的字符串用中文；JSON 结构须合法。

回答：
